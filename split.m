% n=29120;
% idx=randsample(1:size(T,1),n) ;
% CV = T(idx,:) ; % pick rows randomly 
% T(idx,:)=[]; % remove those rows
% CV_labels = T_labels(idx,:) ; % pick rows randomly 
% T_labels(idx,:)=[]; % remove those rows